# ~/.agent-credentials.env   (mode 600)  -- NEVER commit
# Key names are published to the repo during registration.
# Values never leave this file: not into messages, status, logs, or transcripts.
GH_PAT_RESEARCH=
OLCF_PROJECT_ID=
